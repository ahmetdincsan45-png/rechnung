<Window x:Class="Örnek.PdfPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="PDF-Vorschau" Height="900" Width="1200" MinHeight="520" MinWidth="760" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <WrapPanel DockPanel.Dock="Right">
                <Button x:Name="SaveButton" MinWidth="120" MaxWidth="180" Margin="0,0,10,10" Click="Save_Click" Content="Speichern" />
                <Button x:Name="CloseButton" MinWidth="120" MaxWidth="180" Margin="0,0,0,10" Click="Close_Click" Content="Schließen" />
            </WrapPanel>
        </DockPanel>

        <Border Grid.Row="1" Margin="10" Background="#F0F0F0" BorderBrush="#DDD" BorderThickness="1">
            <wv2:WebView2 x:Name="PdfView" />
        </Border>
    </Grid>
</Window>

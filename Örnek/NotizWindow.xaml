<Window x:Class="Örnek.NotizWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
         Title="Hinweis" Height="320" Width="520" MinWidth="360" MinHeight="240" WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="NotizTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <WrapPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveButton" MinWidth="110" MaxWidth="150" Margin="0,0,10,10" Click="Kaydet_Click" Content="Speichern"/>
            <Button x:Name="CancelButton" MinWidth="110" MaxWidth="150" Margin="0,0,0,10" Click="Iptal_Click" Content="Abbrechen"/>
        </WrapPanel>
    </Grid>
</Window>
